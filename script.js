function sexo(){
    if (btn-masculino 'true'){
        valor = formGroupPeso / (formGroupAltura*2);
        valor = formGroupPeso / valor;
        alert = valor;
    }else
    if (btn-feminino 'true'){
        valor = formGroupPeso / (formGroupAltura*2);
        valor = formGroupPeso / valor;
        alert = valor;
    }else
}